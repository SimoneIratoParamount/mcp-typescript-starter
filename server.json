{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "mcp-typescript-starter",
  "description": "Meal planner MCP server — restaurant recommendations with weather, hunger level, and embedded React UI",
  "title": "Meal Planner MCP",
  "repository": {
    "url": "https://github.com/SamMorrowDrums/mcp-typescript-starter",
    "source": "github"
  },
  "version": "1.0.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "mcp-typescript-starter",
      "transport": {
        "type": "stdio"
      },
      "runtimeHint": "node",
      "runtime": "node",
      "command": ["npx", "-y", "mcp-typescript-starter"]
    }
  ],
  "remotes": [
    {
      "type": "streamable-http",
      "url": "http://localhost:3000/mcp",
      "description": "Local HTTP transport — run `pnpm run start:http` after `pnpm build` (set PORT / API keys in `.env`)"
    }
  ]
}
